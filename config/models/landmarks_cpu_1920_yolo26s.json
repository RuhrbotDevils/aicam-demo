{
    "display_name": "YOLO26s Landmarks 1920 (CPU)",
    "id": "landmarks_1920_yolo26s_cpu",
    "scope": "object_detection",
    "active": true,
    "runtime": "pytorch",
    "input": {
        "width": 1088,
        "height": 1920,
        "format": "RGB"
    },
    "model_path": "models/landmarks_1920_yolo26s.pt",
    "notes": "Field-landmark detector exposed as an object-detection model so users can preview landmark detections (corners, T-junctions, lines, etc.) in the Object Detection tab. Runs on CPU via Ultralytics - slow (~1 fps at 1088x1920). The model emits class IDs / labels baked into the .pt; no class_map is applied so all detected landmark types render in the preview.",
    "inference_fps": 1
}
